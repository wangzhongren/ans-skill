---
name: ans-governed-construction
description: 按角色的文件范围开发和修复代码，遵守五层调用，并用测试和文档说明改动。适用于需要控制修改范围的项目开发；不用于只回答问题或普通文案编辑。
---

# ANS Skill：给项目开发定边界

这个技能要解决三件事：**谁能改文件、代码应该放在哪、改完怎样证明没弄坏。**它不会因为看到了整个仓库，就允许 AI 修改整个仓库。下面是便于阅读的中文说明；执行时仍以 [SKILL.md](SKILL.md) 和对应角色的 `boundary.md` 为准。

## 1. 谁能改：先看角色，再看文件

一个项目可以有项目角色、业务能力角色和装配角色。每个角色目录有：

- `role-card.md`：这个角色负责什么、开始工作前要读什么。
- `boundary.md`：**第 1 节逐项列出这个角色可修改的文件**。没列出的文件默认只读。

新项目先由内置的 ANS Governance 角色拟好角色卡和文件范围，交给用户确认。确认后，项目角色负责整体设计、任务安排和验收；业务角色只写自己范围内的源码与测试；装配角色负责根目录的启动文件和整体运行测试。角色切换、派发工作需要用户已给出的同意，或用户认可的项目配置，不能因为文件属于某角色就自动切过去。

每个角色开始改文件前，在技能目录运行：

```sh
python3 scripts/check_dashboard_sync.py --root /absolute/path/to/project
```

如果项目还没配置云端，提醒并问用户是否需要；同一项目得到过回答就不要反复问。已配置但同步进程停了，应说明云端可能不是最新；运行中就复用，不启动第二份。这个检查不会读取项目 Key。详细规则见[角色卡写法](references/role-card.md)和[项目角色](references/project-role.md)。

## 2. 代码放哪：五层是调用规则，不是五个角色

| 层 | 用白话说 | 例子 |
| --- | --- | --- |
| **Interface** | 接收外部输入并返回结果 | 按钮、HTTP 接口、命令行入口 |
| **Pipeline** | 决定先做哪步、遇到分支走哪步 | 创建工单后发送通知 |
| **Service** | 处理业务规则 | 判断工单能否进入下一状态 |
| **Provider** | 访问外部资源 | 读写 SQLite、调用第三方接口 |
| **Model** | 大家共用的数据定义和校验 | 工单字段、状态值 |

运行时只能沿 `Interface → Pipeline → Service → Provider` 向下一层调用；Model 可由各层使用。实现新能力通常先定义 Model，再做 Provider、Service、Pipeline、Interface；查问题则从报错的入口向下追。**一个简单功能不用在五层各建一个文件**，现有入口和能力能完成就复用。

新建 Provider 和 Service 时，`abstract/` 放调用约定，`impl/` 放具体实现，`public/` 是上层唯一允许使用的入口。`utils/` 放通用小工具，`resource/` 放静态资源，`test/` 放测试，根 `main` 文件只负责启动 Interface；它们都不是新的一层。详细依赖规则见[五层说明](references/architecture.md)。

## 3. 流程必须先讲明白“在干嘛”

一条流程首先要让没看过代码的人明白：

1. **要做什么**：谁要完成什么事，系统帮他达到什么结果。
2. **成功后怎样**：用户看到什么、数据发生什么变化。
3. **失败时怎样**：用户看到什么、哪些数据没有保存或改变。

这些写在项目理解的 `flow.intent`。比如“保存工单草稿”不能只写“从页面入口到处理结果”；只有源码证实后，才能写成“用户暂不提交时保存当前内容为草稿，离开页面后还能继续编辑”。按钮点击写在 `triggers`；实际 `if/else` 写在 `graph` 的判断节点和有条件的连线上；错误节点写代码位置和“出问题先检查什么”。不要猜源码里没有的分支或结果。旧流程只有步骤时，页面只画线性流程并提示分支尚未记录。格式和命令见[项目理解说明](references/project-context.md)。

旧流程没写用途时，页面会标“用途待补充”。不要按 50 个按钮名称一次性生成 50 段套话；只检查当前任务相关的入口、业务代码和测试，核实后逐条补。其余保持待补充。

项目只有一份 `project-context/context.sqlite3`；各角色只查询和修改自己的 `role_id` 记录。它是帮助人和 AI 找到流程、数据、接口的索引，不能取代真实代码、接口约定或带日期的设计文档。

## 4. 改完怎样确认

只测试这次实际改动的层。先测底层真实行为，再接上一层；最后从用户入口跑一遍。每个角色的测试放在项目 `test/<角色>/`，并写好与改动对应的设计、功能、修改或修复文档。测试失败就不能写“已验证”；底层约定变化后，受影响上层要重新测。详见[测试要求](references/testing.md)和[文档规则](references/documentation.md)。

多人协作如果要记录阶段计划、派发、反馈和验收，项目角色用 `task_ops`。它需要用户审核过的项目配置；AI 不能自己算个哈希就声称用户已批准。它写入任务状态供 Dashboard 展示，但不会自动启动代理。云端管理员在网页点“批准”，也不会自动给本地写代码的权限。详见[任务操作说明](references/task-operations.md)。

## 5. Dashboard 和云端各做什么

- **本地 Dashboard**：在本机只读查看角色、项目理解、任务状态和协作记录。项目角色获确认后就可启动；没有任务记录时显示为空，不编造进度。
- **云端 Dashboard**：多人查看时才配置。管理员用本地项目 ID 创建 Key；项目根目录用 `.ans-dashboard.local.json` 保存 Key。同步器只把展示所需的摘要从本地上传到云端，不上传完整源码、角色文档全文、原始 SQLite 文件或 Key。
- **同步状态**：`scripts/check_dashboard_sync.py` 看本机进程是否在运行、上次上传是否成功；运行中不等于云端一定最新。
- **云端消息**：角色可用同一个项目 Key 发消息和申请，并声明自己的角色 ID；服务器只能确认项目，不能单独证明具体是哪位角色。服务器消息可按需查询，目前不会自动写回本地项目。

启动、配置、同步和部署命令见[入门总览](doc/guide/2026-09-25_guide_ans-skill-overview.md)与[Dashboard 说明](dashboard/README.md)。旧式全项目代码图谱默认关闭，只有用户明确要求时才生成。

## 6. AI 怎么说话

假设读者刚学编程。先说改了什么、用户会看到什么，再说原因和怎么验证。句子短一点；必要术语第一次出现就解释。几件事并列时用 `1. 2. 3.`，不要堆“治理能力、候选状态、依赖证据”这种读完仍不知道要做什么的词。没有核实的结果直接说“还没核实”，不要用套话填空。

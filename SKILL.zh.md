---
name: ans-governed-construction
description: Govern repository implementation, repair, and refactoring with role cards, module boundary documents, explicit mutation scopes, architecture-aware capability boundaries, dual static/dynamic verification gates, and evidence invalidation. Use when a task requests bounded AI code changes, architecture enforcement, protected or frozen components, evidence-based acceptance, or ANS-style software construction governance. Do not use for read-only explanation, ordinary content editing, or unconstrained brainstorming.
---

# ANS 受控构建规范

将软件构建视为从可信基线到候选状态之间的受控过渡。AI 可以广泛阅读以理解系统，但只能修改当前任务授权的工件。

核心原则：

> 知识可及全局；修改权限止于局部。

AI 拥有提案权。客户和执行环境保留授权与提交权。

首次调用时，你默认工作在 **ANS Governance** 内置角色下——无需先创建角色卡或边界文档即可开始工作。见[内置角色：ANS Governance](#内置角色ans-governance)。

## 治理不变量

整个任务期间必须维护以下不变量：

1. **有界变更：** 每个被修改的工件必须属于当前变更范围，或属于已明确批准的扩展范围。
2. **架构即能力：** 组件只能依赖或调用其架构角色与已声明契约所允许的能力。
3. **提案不等于接受：** 生成的补丁在两道验证门通过之前，都只是候选。
4. **证据有来源：** 验证证据仅对产生它的确切候选体、环境、输入和策略有效。
5. **冻结即受保护，非永久：** 不应在普通下游修复中修改冻结工件。仅当任务明确授权上游演进时方可更改。
6. **上游变更使下游证据失效：** 当依赖或契约发生变更时，受影响下游模块的既有证据变为过时，需重新验证。
7. **不得静默弱化治理：** 不得通过删除测试、放宽断言、扩大权限、压制失败或改写治理元数据来使候选通过，除非客户明确要求修改该策略。

### 严格相邻层调用与共享 Model

```text
Interface -> Pipeline -> Service -> Provider -> 外部基础设施
Model：由全部四个操作层共享
```

完整依赖决策表、三层内部结构（`abstract/`/`public/`/`impl/`）、执行边界细节（import、回调、工厂、动态 import、re-export）、创建本地化规则和 Model 共享约束见 [references/architecture.md](references/architecture.md)。

## 角色卡与模块边界文档

角色卡（Role Card）和模块边界文档（Module Boundary Document）共同定义 **AI 可以读取什么、可以修改什么**。它们是每个项目的独立工件，由 AI 在需要时自主创建。所有工件在人工接受之前均为候选。语言与命名依次遵循用户明确要求、仓库约定、语言和工具链惯例；系统区域设置仅作为自然语言说明的兜底，不据此翻译标识符或改名。

### 模块边界文档（Module Boundary Document）

每个模块一份结构化 markdown 文档（`boundary.md`），包含：

- **第 1 节：可以操作的文件与文件夹** — 变更白名单。一张表格列出 AI 可以创建或修改的每个文件或目录，标注类型和功能。这是变更范围的唯一权威来源。
- **伴随文档 `functional-description.md`** — 架构图、按功能组织的文件表（含归属和权限级别），以及跨模块职责澄清。（内置治理边界文档是例外：它直接以第 2 节及以后承载职责说明。）

**未列入第 1 节的文件默认只读。** 要修改未列入的文件，AI 必须首先提议更新边界文档的第 1 节。此提议遵循与代码变更相同的证据和验收门禁（治理不变量 #1）。

项目只维护一份 [`project-context/context.sqlite3`](references/project-context.md)。每个角色按 `role_id` 先读自己的总览，再查询相关主题；新增、修改、删除通过统一 CLI 和已接受的行级边界执行。正式契约与源码仍是定义依据，项目理解不扩大修改权限。

### 角色卡（Role Card）

每张角色卡标识一个智能体角色及其范围边界，包含：

1. **角色描述** — 该智能体负责什么（以及不负责什么）。
2. **必读文档** — 链接到：
   - 相关的模块边界文档：第 1 节是变更白名单。
   - 仓库约定（如 `AGENTS.md`）。
   - 适用的技能定义（如本 SKILL.md）。
3. **执行原则** — 该角色下的每项任务必须遵守以下规则：

   - **第 1 节是唯一的变更授权。** 只有边界文档第 1 节列出的文件才能被修改。角色卡不复制、不扩大、不合并其他角色的权限。
   - **只修改任务需要的内容。** 被列入白名单的目录不授权对其下所有文件的批量修改。
   - **跨范围变更回到客户。** 任务需要修改第 1 节之外的文件时，报告文件、原因和影响。客户派发独立角色评估。未获明确授权前不得修改。
   - **默认只读。** 未列出即只读，除非第 1 节另有说明。
   - **实现顺序只在本角色内适用。** Model → Provider → Service → Pipeline → Interface 的构建顺序作用于本角色的受影响工作，不是跨角色的执行顺序。
   - **按依赖就绪状态调度。** 契约已确定、写入范围不重叠且依赖就绪的角色可并行；其余按依赖顺序推进。无并行工具时顺序切换角色。

### 关系

```
角色卡（Role Card）
  └─ 必读 ──→ 模块边界文档（Module Boundary Document）
                 ├─ 第 1 节：变更白名单
                 ├─ 未列出文件 → 只读
                 └─ 伴随文档 functional-description.md：上下文与归属
```

### 初始创建流程（Bootstrap）

当 AI 首次遇到没有边界文档的项目时——这属于 **ANS Governance** 内置角色下的任务，受全部七条治理不变量约束（完整路径见 [bootstrap 工作流](references/bootstrap-workflow.md)）：

1. 分析项目结构，识别能力模块及其架构层足迹（#2）。
2. 为每个能力模块草拟一份模块边界文档，第 1 节列出初始白名单；描述、图与归属写入伴随文档 `functional-description.md`。白名单不得超过该能力的实际范围（#1、#7）。
3. 为每个能力模块创建一张角色卡，引用其边界文档。
4. 将所有工件作为候选提交人工审批（#3）。未获批前不得进入代码变更。

### 治理规则

- 每张角色卡必须引用其模块边界文档。每份边界文档必须在第 1 节列出具体的应用源码文件，不得使用通配符目录；支持性与治理位置（`docs/`、角色目录、技能 `references/`）可使用目录级或条件条目。
- 在能力模块中添加或移除文件时，必须同时更新其边界文档的第 1 节。不一致即为文档缺口 — 标记或阻止验收。
- 角色卡提供上下文，而非变更授权。只有所引用边界文档的第 1 节才提供变更授权。
- 边界文档和角色卡遵守与代码变更相同的证据和验收门禁。
- **共享抽象**（Model 类型、事件总线、公共入口、跨模块契约）必须有且仅有一个**主属主**角色。其他角色只能作为只读依赖引用。主属主变更会使所有引用角色的证据失效（治理不变量 #6）。
- **角色激活检查：** 创建或修改任何文件前，确认当前角色身份。没有激活的角色卡时，只能在 ANS Governance 内置角色下创建治理工件。应用代码需要加载角色卡和边界文档第 1 节。在无激活角色状态下写应用代码属于治理违规。

### 内置角色：ANS Governance

系统仅内置一个角色 **ANS Governance**，直接定义于本 SKILL.md 中。它不需要先被创建即可存在。其参考边界文档位于 [references/built-in-boundary.md](references/built-in-boundary.md)。

- **职责：** 创建和维护治理工件 — 角色卡、模块边界文档、以及技能定义文件本身。
- **变更范围：** 仅限治理工件本身。不涉及任何应用代码。具体白名单见内置边界文档第 1 节。
- **使用时机：** 初始创建（无边文档时）、新增能力模块、拆分或合并模块、更新治理规则。
- **执行原则：** 与所有角色相同 — 见[角色卡](#角色卡role-card)。

所有其他角色和边界文档均由 ANS Governance 角色创建。一个角色从不创建自身。每个角色是一个目录，内含角色卡、边界文档和变更日志。见 [references/role-card.md](references/role-card.md) 的目录布局。

**Bootstrap 之后，ANS Governance 不创建任何应用代码。** 每个能力模块的实现在该能力的角色下独立进行。写代码前先加载该角色的角色卡和边界文档第 1 节。

### 项目默认角色

由 ANS Governance 按[项目角色模板](references/project-role.md)创建项目默认角色及其白名单，验收后主聊天框默认保持该角色。它负责全局调查、架构与抽象设计、各角色对接设计文档和调度；仅能写获批设计文档与执行记录，不修改业务源码、测试、抽象接口源码或授权配置。它不是第二个内置角色。

调度角色根据阶段任务图派发能力或装配代理，核对证据后放行后续任务。代码引用图只是依赖线索，不是执行图。能力代理不自行创建子代理。每次启用执行角色、切换身份或派发子代理，都要有客户对此次范围的同意，或匹配客户认可的项目配置预授权。文件归属不是自动执行授权；无子代理工具时也不能绕过此检查。

### 项目默认 Dashboard

使用本技能的每个项目都按 [dashboard.md](references/dashboard.md) 建立可复用看板。项目默认角色验收后，为实际项目启动或复用一个本机只读 Dashboard，能够打开时直接打开并提供地址。页面和服务脚本保留在技能目录，角色卡、协同记录和角色图谱来自当前项目。不得把正式任务指向 `test/game-engine`，也不能复制游戏示例流程冒充新项目功能。用户明确关闭或环境无法运行时说明限制，不阻止其他已授权开发。

## 角色切换

每个代理上下文同一时间只有一个角色处于激活状态；不同代理可以并行持有不同角色。切换角色会改变变更授权、必读文献和执行上下文。

### 激活流程

1. **加载目标角色卡** — 从 `角色卡/<role>/role-card.md` 读取。角色目录根名跟随项目语言（中文项目为 `角色卡/`，英文项目如 `role-cards/`）
2. **加载 boundary.md 第 1 节** — 这成为唯一的变更授权
3. **确认授权与激活** — 核对客户同意或客户认可配置中的匹配规则，记录其范围及版本；确认角色卡已加载且其第 1 节是当前白名单。在无激活角色状态下写应用代码属于治理违规（治理不变量 #1）

角色需显式切换并加载对应白名单后才激活，直到下次显式切换或任务结束。调度时只读其他角色卡不会自动取得该角色权限。只有 Governance 使用内置白名单；项目调度角色必须加载自己的获批角色卡和 boundary.md。

### 切换时机

| 当前角色 | 切换到 | 时机 |
|---|---|---|
| ANS Governance（内置） | 项目调度角色 | 调度与能力角色定义已验收，需要协调执行 |
| 项目调度角色 | 能力或装配角色 | 派发就绪阶段，或进行顺序执行的显式角色切换 |
| 能力或装配角色 | 项目调度角色 | 返回产物、证据或未满足的依赖 |
| 能力工作代理 | 项目默认角色（交接） | 返回跨角色请求，不自行切换身份或取得新权限 |
| 项目默认角色 | ANS Governance（内置） | 客户授权的治理修改；能力工作代理只返回请求 |
| 项目调度角色 | ANS Governance（内置） | 需要新增角色、修改权限或解决归属冲突 |

协调执行在 bootstrap 验收后由治理角色交给项目调度角色，再派发就绪任务。独立单角色任务也必须通过客户同意或配置预授权后激活所属角色；复用已验收定义，不重复 bootstrap。

### 规则

- **每个任务上下文一个角色。** 单个 AI agent 在同一时间只能工作在一个角色下。只有已激活且获批的项目调度角色派发执行代理。各工作代理加载自己的角色卡和任务子范围，不派生代理或调度同伴；无子代理工具时明确顺序切换角色。
- **修改前必须激活角色。** 创建或修改任何文件前确认当前角色。无激活角色卡时只能以 ANS Governance 内置角色创建治理工件。
- **不静默漂移。** 当任务跨越角色边界（如修复一个能力的 bug 时发现治理缺口），暂停跨范围修改并返回项目角色。执行实例锁定原角色，新角色启用必须重新核对客户同意或配置预授权。
- **证据按角色隔离。** 在一个角色下收集的验证证据仅对该角色的变更范围有效。单纯切换角色不使证据失效。调度角色可以核对各角色的验证证据来放行阶段，但不转移其实现与验证责任，也不将单个角色通过等同于集成通过。
- **装配角色也是能力角色。** 它遵循同样的切换规则。它可提前准备构建与测试配置；最终集成等待实际依赖及其验证完成，不等待无关角色。装配前读取参与角色的 `api-spec.md`，所修改文件仍遵守自身所属层的调用规则，见[装配规则](references/built-in-assembly.md)。

## 任务路由

由项目调度角色将协调执行任务路由到正确的已验收角色；缺少角色或需要改归属时回到治理角色。独立单角色任务也必须经过客户同意或配置预授权后激活所属角色。

### 路由数据源

主要路由表来自每个角色的 `boundary.md` 第 1 节。项目中的每个文件归属且仅归属一个角色，或未归属。通过定向源码搜索、调用堆栈和角色对接契约追踪依赖，不要求代码图谱扫描。

### 路由流程

1. **提取文件证据。** 从 bug 报告或任务描述中提取受影响的文件、错误位置、堆栈或用户可见症状。优先使用具体文件路径，而非抽象描述。
2. **查询归属角色。** 检索所有 `角色卡/*/boundary.md` 第 1 节表，查找受影响文件的所属角色。
   - **找到文件** → 路由到该角色。按[角色切换](#角色切换)加载该角色的角色卡并激活。
   - **文件未列出** → 该文件默认只读。报告缺口并决定：加入现有角色的第 1 节、创建新角色、或标记为冻结。
   - **无文件证据** → 从最高相关入口开始调查，按[工作流](references/workflow.md)的自顶向下追踪。通过定向源码搜索与契约检查追踪调用者和依赖。
3. **检查跨角色影响。** 结合相关调用方源码与对接契约，判断影响是否跨越多个角色。如果影响限定在一个角色内，直接路由。如果跨越边界：
   - 将任务拆分为独立子任务，各自路由到其归属角色。
   - 或将主要修复路由到归属角色，并将跨角色依赖报告为后续事项。

### 示例

| Bug 报告 | 文件证据 | 归属角色 |
|---|---|---|
| "订单导出超时" | `services/order/export.ts` | 订单服务 |
| "登录页面 500" | `interface/http/auth.ts` | 用户认证 |
| "数据库查询返回错误结果" | `providers/database/query.py` | 数据存储 |
| UI 崩溃，无文件证据 | 从实际启动入口 → Interface → Pipeline → Service → Provider 追踪 | 追踪后路由 |

### 路由失败

如果文件不在任何角色的第 1 节中，且尚未执行 bootstrap，则进入 [ANS Governance 内置角色](#内置角色ans-governance)，先执行 [bootstrap](references/bootstrap-workflow.md)。

每个角色是一个目录，内含角色卡、模块边界文档和变更日志。文件名跟随项目语言。变更日志按倒序记录该角色的创建、范围更新、拆分或合并——自包含，无需翻阅其他文件。

## 必读文献路径

在对应阶段只读相关文献。这些是强制任务路径，不是可选建议。各层参考文献（model、provider、service、pipeline、interface）以及 testing、documentation 通过角色卡的 [Required reading](references/role-card.md) 加载。未读取的文献绝不授权绕过全局规则。

| 文献 | 读取时机 |
| --- | --- |
| [项目默认角色](references/project-role.md) | 全局调查、设计角色对接文档、角色启用授权检查 |
| [角色功能图谱](references/role-atlas.md) | 客户要求角色总览、选择功能逐步演示或查看步骤源码依据 |
| [本地角色看板](references/dashboard.md) | 项目交接与恢复时默认启动/复用，查看角色阶段记录并交付项目地址 |
| [统一任务入口](references/task-operations.md) | 初始化/派发、授权与版本检查、反馈、测试证据、验收及恢复 |
| [项目与执行角色协同表](references/coordination.md) | 进度、问题、完成反馈及需求/设计变更的同步与确认 |
| [项目调度角色模板](references/scheduler.md) | 创建项目调度角色，或激活调度角色规划、派发、恢复与放行阶段时 |
| [行动与沟通](references/action-policy.md) | 开始任务或决定采取何种动作（执行、检查、预览、询问、通知）时；校准动作与既有授权和证据 |
| [工作流](references/workflow.md) | 任何设计、调研或变更：基线、自底向上构建、自顶向下诊断、变更契约、范围扩展和停止条件 |
| [模块边界文档第 1 节](references/module-boundary.md) | 每项任务之前：加载第 1 节确定变更范围 |
| [角色卡](references/role-card.md) | 每项任务之前：加载角色卡了解能力上下文、执行原则和必读文献 |
| [证据与验收](references/evidence.md) | 冻结工件或变更的依赖，以及在接受或报告任何候选完成之前 |

将仓库名称映射到五层职责；命名差异不放松依赖。报告既有冲突并在必要时限定修复范围，而不是静默添加例外或迁移不相关代码。文件名和导出机制跟随项目语言；`main.js` 和 `index.js` 不是通用要求。

## 代码图谱默认关闭

日常开发、调查和验收不加载图谱参考、不扫描、更新或查询图谱快照，也不自动打开图谱。仅在客户明确要求时使用保留的[图谱工具](references/code-atlas.md)。任务依赖图和角色 Dashboard 继续使用协同记录，不依赖代码图谱。
